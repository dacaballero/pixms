<% title(@publication.campaign.customer.name) %>
<% subtitle(@publication.campaign.name) %>


<%= form_for(@publication, :html => { :class => "form-horizontal"}) do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>
